import React from 'react'
import { render } from '../../test-utils'
import PieUsageWidget from './PieUsageWidget'

describe('PieUsageWidget', () => {
    it('renders correctly', () => {
        render(<PieUsageWidget />)
    })
})
