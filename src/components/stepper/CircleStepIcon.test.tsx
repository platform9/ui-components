import React from 'react'
import { render } from '../../test-utils'
import CircleStepIcon from './CircleStepIcon'

describe('CircleStepIcon', () => {
    it('renders correctly', () => {
        render(<CircleStepIcon />)
    })
})
