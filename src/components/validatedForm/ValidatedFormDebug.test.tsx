import React from 'react'
import { render } from '../../test-utils'
import ValidatedFormDebug from './ValidatedFormDebug'

describe('ValidatedFormDebug', () => {
    it('renders correctly', () => {
        render(<ValidatedFormDebug />)
    })
})
