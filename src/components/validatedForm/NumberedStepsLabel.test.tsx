import React from 'react'
import { render } from '../../test-utils'
import NumberedStepsLabel from './NumberedStepsLabel'

describe('NumberedStepsLabel', () => {
    it('renders correctly', () => {
        render(<NumberedStepsLabel />)
    })
})
