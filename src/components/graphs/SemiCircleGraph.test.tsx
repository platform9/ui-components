import React from 'react'
import { render } from '../../test-utils'
import SemiCircleGraph from './SemiCircleGraph'

describe('SemiCircleGraph', () => {
    it('renders correctly', () => {
        render(<SemiCircleGraph />)
    })
})
