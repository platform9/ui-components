import React from 'react'
import { render } from '../../test-utils'
import CodeMirrorModal from './CodeMirrorModal'

describe('CodeMirrorModal', () => {
    it('renders correctly', () => {
        render(<CodeMirrorModal />)
    })
})
