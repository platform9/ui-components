import React from 'react'
import { render } from '../../test-utils'
import PageContainerHeader from './PageContainerHeader'

describe('PageContainerHeader', () => {
    it('renders correctly', () => {
        render(<PageContainerHeader />)
    })
})
