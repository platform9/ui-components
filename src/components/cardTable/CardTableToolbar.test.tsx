import React from 'react'
import { render } from '../../test-utils'
import CardTableToolbar from './CardTableToolbar'

describe('CardTableToolbar', () => {
    it('renders correctly', () => {
        render(<CardTableToolbar />)
    })
})
