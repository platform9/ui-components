import React from 'react'
import { render } from '../../test-utils'
import helpers from './helpers'

describe('helpers', () => {
    it('renders correctly', () => {
        render(<helpers />)
    })
})
