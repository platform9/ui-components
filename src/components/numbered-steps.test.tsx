import React from 'react'
import { render } from '../test-utils'
import NumberedSteps from './numbered-steps'

describe('numbered-steps', () => {
    it('renders correctly', () => {
        render(<NumberedSteps />)
    })
})
