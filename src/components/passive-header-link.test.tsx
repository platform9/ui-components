import React from 'react'
import { render } from '../test-utils'
import PassiveHeaderLink from './passive-header-link'

describe('passive-header-link', () => {
    it('renders correctly', () => {
        render(<PassiveHeaderLink />)
    })
})
