import React from 'react'
import { render } from '../../test-utils'
import portals from './portals'

describe('portals', () => {
    it('renders correctly', () => {
        render(<portals />)
    })
})
