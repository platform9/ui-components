import React from 'react'
import { render } from '../../test-utils'
import Tooltip from './Tooltip'

describe('Tooltip', () => {
    it('renders correctly', () => {
        render(<Tooltip />)
    })
})
