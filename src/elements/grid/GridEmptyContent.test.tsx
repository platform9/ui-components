import React from 'react'
import { render } from '../../test-utils'
import GridEmptyContent from './GridEmptyContent'

describe('GridEmptyContent', () => {
    it('renders correctly', () => {
        render(<GridEmptyContent />)
    })
})
