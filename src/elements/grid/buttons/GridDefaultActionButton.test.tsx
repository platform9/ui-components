import React from 'react'
import { render } from '../../../test-utils'
import GridDefaultActionButton from './GridDefaultActionButton'

describe('GridDefaultActionButton', () => {
    it('renders correctly', () => {
        render(<GridDefaultActionButton />)
    })
})
