import React from 'react'
import { render } from '../../../test-utils'
import GridDefaultDeleteButton from './GridDefaultDeleteButton'

describe('GridDefaultDeleteButton', () => {
    it('renders correctly', () => {
        render(<GridDefaultDeleteButton />)
    })
})
