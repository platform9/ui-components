import React from 'react'
import { render } from '../../../test-utils'
import getGridRedirectButton from './getGridRedirectButton'

describe('getGridRedirectButton', () => {
    it('renders correctly', () => {
        render(<getGridRedirectButton />)
    })
})
