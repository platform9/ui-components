import React from 'react'
import { render } from '../../test-utils'
import GridRowMenu from './GridRowMenu'

describe('GridRowMenu', () => {
    it('renders correctly', () => {
        render(<GridRowMenu item={{}} rowMenuItems={[]} />)
    })
})
