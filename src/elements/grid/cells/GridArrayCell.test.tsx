import React from 'react'
import { render } from '../../../test-utils'
import GridArrayCell from './GridArrayCell'

describe('GridArrayCell', () => {
    it('renders correctly', () => {
        render(<GridArrayCell />)
    })
})
