import React from 'react'
import { render } from '../../test-utils'
import GridSearchFilter from './GridSearchFilter'

describe('GridSearchFilter', () => {
    it('renders correctly', () => {
        render(<GridSearchFilter />)
    })
})
