import React, { ReactNode } from 'react';
interface DataPointLineProps {
    children: ReactNode;
    className?: string;
    lineColor?: string;
    arrowColor?: string;
}
interface DataPointLineProps {
    children: ReactNode;
    className?: string;
    lineColor?: string;
    arrowColor?: string;
}
export default function DataPointLine({ className, lineColor, arrowColor, children, }: React.PropsWithChildren<DataPointLineProps>): JSX.Element;
export {};
